import { StyleSheet } from "react-native"

/** Shared layout styles used across the app. */
export const sharedStyles = StyleSheet.create({
  flex1: { flex: 1 },
  flexWrap: { flexWrap: "wrap" },
  row: { flexDirection: "row" },
})
